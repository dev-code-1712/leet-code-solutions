class Solution {
    public int[] shuffle(int[] nums, int n) {
        int[] result = new int[nums.length];

        for(int i=0,j=0; i<2*n; i+=2, j++) {
            result[i] = nums[j];
            result[i+1] = nums[n + j];
        }

        return result;
    }
}
