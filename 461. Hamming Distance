class Solution {
    public int hammingDistance(int x, int y) {
        int t = x ^ y;
        int count = 0;
        while(t > 0) {
            if((t & 1) == 1)
                count ++;
            
            t >>= 1;
        }

        return count;
    }
}
