class Solution {
    public boolean isPowerOfTwo(int n) {
        if(n == 1 || n == 2)
            return true;
        
        long power = 1;
        while(power < n) {
            power = power * 2;
            //System.out.println(power);
        }
        return n == power? true : false;
    }
}
