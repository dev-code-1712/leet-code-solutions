class Solution {
    public String restoreString(String s, int[] indices) {
        char[] vals = new char[indices.length];
        for(int i=0; i<indices.length; i++) {
            vals[indices[i]] = s.charAt(i);
        }

        return String.valueOf(vals);
    }
}
