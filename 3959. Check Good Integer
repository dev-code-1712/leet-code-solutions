class Solution {
    public boolean checkGoodInteger(int n) {
        int digits = 0, squares = 0;
        while(n > 0) {
            int d = n%10;
            digits += d;
            squares += d*d;

            n/= 10;
        }

        return (squares - digits) >= 50;
    }
}
