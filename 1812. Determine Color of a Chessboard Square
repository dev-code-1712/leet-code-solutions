class Solution {
    public boolean squareIsWhite(String coordinates) {
        int l = coordinates.charAt(0) - 96;
        int d = coordinates.charAt(1) - 47;
        if( (l%2 == 0 && d % 2 == 0) || (l%2 == 1 && d % 2 == 1))
            return true;
        else
            return false;
    }
}
